package main
import (
	// fm "fmt"
	// str "strings"

	// "strconv"

)

func Handle_string() {
	// var text string = "This is an example of a string"

	// strings.HasPrefix(s, prefix string) bool: Check xem chuỗi s bắt đầu bằng prprefix hay không
	// strings.HasSuffix(s, suffix string) bool: Check xem chuỗi s có kết thúc bằng suffix hay không

	// strings.Contains(s, substr string) bool: Check xem có chuỗi substr tồn tại trong chuỗi s hay không
	// strings.Index(s, str string) int: Trả về index của ký tự str đầu tiên trong chuỗi, nếu k có thì là -1
	// strings.LastIndex(s, str string) int: Trả về index của ký tự str xuất hiện cuối cùng trong chuỗi, nếu k có thì -1
	// strings.Replace(str, old, new string, n int): Thay thế chuỗi
	// strings.Count(s, str string) int: Đếm số lần xuất hiện chuỗi con
	// strings.Repeat(s, count int) string: Trả về chuỗi mới gồm count bản sao của chuỗi s
	// ToLower(s) string: Viết thường
	// ToUpper(s) string: Viết hoa

	// strings.TrimSpace(s): Bỏ khoảng trắng ở đầu cuối chuỗi
	// string.Trim(s, str): Cắt chuỗi str từ chuỗi s
	// strings.Split(s, sep): Tách chuỗi bằng sep
	// strings.Join(sl []string, sep string): trả về một chuỗi chứa tất cả các phần tử trong slice sl, được phân tách bằng sep


	// Chuyển đổi 1 kiểu dữ liệu sang chuỗi và ngược lại

	// strconv.Itoa(i int) string: Chuyển đổi số i sang chuỗi
	// strconv.FormatFloat(f float64, fmt byte, prec int, bitSize int) string: Đổi float sang chuỗi

	// strconv.Atoi(s string) (i int, err error): Chuyển chuỗi sang số
	// strconv.ParseFloat(s string, bitSize int) (f float64, err error): Chuỗi sang float 64bit

}	